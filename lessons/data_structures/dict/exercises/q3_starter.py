"""Count word frequencies in a string.

Do NOT import Counter.
Build the dict manually.
"""
from __future__ import annotations


def word_count(text: str) -> dict[str, int]:
    """Return a dict mapping each word to its frequency in text.

    Split on whitespace. Case-sensitive. Empty string returns {}.

    Examples:
        word_count("the cat sat on the mat")
        # {"the": 2, "cat": 1, "sat": 1, "on": 1, "mat": 1}

        word_count("") == {}
        word_count("hello hello") == {"hello": 2}
    """
    pass
