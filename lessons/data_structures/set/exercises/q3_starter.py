"""Find elements that appear more than once in a list.

Use sets for O(n) performance.
Return results sorted in ascending order.
"""
from __future__ import annotations


def find_duplicates(lst: list) -> list:
    """Return a sorted list of elements that appear more than once.

    Each duplicate appears only once in the output.

    Examples:
        find_duplicates([1, 2, 3, 2, 4, 3]) == [2, 3]
        find_duplicates([1, 2, 3])            == []
        find_duplicates([])                   == []
        find_duplicates([5, 5, 5])            == [5]
    """
    pass
